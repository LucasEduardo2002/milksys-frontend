E2E MQTT / Coleta — resultado

Data: 26/05/2026

Ações executadas:
- Acessada a Página Inicial (/pagina-inicial).
- Selecionado um produtor (primeiro item), preenchido `Tanque=99` e `Acidez=20` e clicado em `Cadastrar`.

Observações e resultados:
- A operação inseriu um novo registro no histórico: tabela mostra nova linha com tanque `99`.
- Contagem do histórico atualizou de "1–10 de 181" para "1–10 de 182" indicando que a coleta foi persistida no banco.
- O broker MQTT estava acessível (porta 8082) durante o teste.
- Nos logs do backend não foram identificadas mensagens explícitas de publish/subscribe (o `server.js` não imprimiu logs extras), possivelmente porque o processamento MQTT ocorre em outro serviço ou com logs silenciosos.

Conclusão:
- Fluxo completo de cadastro → API → persistência no banco funcionou.
- Para validação end-to-end da fila MQTT (publish/consume/ack), recomendo:
  1) habilitar logs detalhados no backend para eventos MQTT,
  2) observar logs do broker (se disponível),
  3) reexecutar E2E e capturar publish/subscribe no broker.

Arquivos relacionados:
- playwright-summary.txt
- report.md

Se quiser, eu: 
- ativo logs detalhados no backend e repito o teste para capturar publish/subscribe, ou
- implemento testes Playwright que verifiquem publish/consume (preciso de logs/endpoint do broker).